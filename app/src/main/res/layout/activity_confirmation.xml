<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/gradient_color_main"
    tools:context="com.example.caio.shoppinghelper.activities.ConfirmationActivity">

    <TextView
        android:id="@+id/confirmation_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="155dp"
        android:textColor="@color/black"/>

    <EditText
        android:id="@+id/confirmation_box"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:background="@drawable/border_to_box"
        android:ems="10"
        android:hint="@string/sms_hint"
        android:textColorHint="@color/hintTextColor"
        android:inputType="textPersonName"
        android:maxLength="@integer/Sms_text_length"
        android:padding="15dp"
        android:textAlignment="center"
        />

    <Button
        android:id="@+id/button_confirmation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/confirmation_box"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="33dp"
        android:background="@drawable/button_border"
        android:text="@string/butto_confirmation"
        android:textColor="@color/black"/>

    <ProgressBar
        android:id="@+id/progressBarConfirmation"
        style="@style/AlertDialog.AppCompat.Light"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_centerInParent="true"
        android:layout_gravity="center_horizontal"
        android:indeterminate="true"
        android:indeterminateDrawable="@drawable/progress_bar"
        android:visibility="invisible"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"/>

</RelativeLayout>
